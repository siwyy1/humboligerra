services:

  pytest:
    build: ./
    image: python_custom
    container_name: pytest2
    volumes:
      - ./:/app
    command: >
      uv run pytest

  dbttest:
    build: ./
    image: python_custom
    container_name: dbttest2
    volumes:
      - ./:/app
    command: >
      uv run dbt test
